"use client";

import { useEffect, useMemo, useState } from "react";
import Link from "next/link";
import { CalendarDays, Search, SlidersHorizontal } from "lucide-react";
import { supabase } from "@/lib/supabase/client";

type Appointment={id:string;client_name:string;client_phone:string;client_email:string|null;start_at:string;end_at:string;status:string;source:string;service_category:string|null;booking_duration:number|null;price_cents:number|null;pricing_type:string|null;service:{name:string;price_label:string;category:string}|null;staff:{name:string}|null};
type Staff={id:string;name:string};
type Service={id:string;name:string;category:string};
const statuses=["all","pending","confirmed","in_progress","completed","cancelled","no_show"];
const statusLabels:Record<string,string>={all:"Todos los estados",pending:"Pendiente",confirmed:"Confirmada",in_progress:"En curso",completed:"Completada",cancelled:"Cancelada",no_show:"No se presentó"};
const sourceLabels:Record<string,string>={all:"Todos los orígenes",public_booking:"Reserva pública",gloria_access:"Gloria Access",gloria_hub:"Gloria Hub",staff_created:"Creada por staff",manual_admin:"Manual (admin)"};

export default function HubAppointmentsPage(){
  const [items,setItems]=useState<Appointment[]>([]);const [loading,setLoading]=useState(true);const [staff,setStaff]=useState<Staff[]>([]);const [services,setServices]=useState<Service[]>([]);const [query,setQuery]=useState("");const [status,setStatus]=useState("all");const [staffId,setStaffId]=useState("all");const [serviceId,setServiceId]=useState("all");const [source,setSource]=useState("all");const [selected,setSelected]=useState<Appointment|null>(null);const [actionMessage,setActionMessage]=useState<string|null>(null);
  async function load(){setLoading(true);const [{data:a},{data:st},{data:sv}]=await Promise.all([supabase.from("appointments").select("id,client_name,client_phone,client_email,start_at,end_at,status,source,service_category,booking_duration,price_cents,pricing_type,service:service_id(name,price_label,category),staff:staff_id(name)").order("start_at",{ascending:true}),supabase.from("staff").select("id,name").eq("active",true).order("name"),supabase.from("services").select("id,name,category").eq("active",true).order("name")]);setItems(((a as unknown) as Appointment[])||[]);setStaff(((st as unknown) as Staff[])||[]);setServices(((sv as unknown) as Service[])||[]);setLoading(false)}
  useEffect(()=>{load()},[]);
  const filtered=useMemo(()=>items.filter(a=>{const q=query.trim().toLowerCase();const matchesQ=!q||a.client_name.toLowerCase().includes(q)||(a.client_phone||"").toLowerCase().includes(q)||(a.client_email||"").toLowerCase().includes(q);return matchesQ&&(status==="all"||a.status===status)&&(staffId==="all"||a.staff?.name===staff.find(s=>s.id===staffId)?.name)&&(serviceId==="all"||a.service?.name===services.find(s=>s.id===serviceId)?.name)&&(source==="all"||a.source===source)}),[items,query,status,staffId,serviceId,source,staff,services]);
  async function changeStatus(id:string,next:string){setActionMessage(null);const reason=next==="cancelled"?window.prompt("Motivo de la cancelación (opcional)")||null:null;if(["cancelled","no_show"].includes(next)&&!window.confirm(`¿Marcar esta cita como ${(statusLabels[next]||next).toLowerCase()}?`))return;const {error}=await supabase.rpc("hub_update_appointment_status",{p_appointment_id:id,p_status:next,p_reason:reason});if(error){setActionMessage(error.message);return;}await load();setSelected(v=>v?.id===id?{...v,status:next}:v)}
  return <div><div className="flex flex-col lg:flex-row lg:items-end lg:justify-between gap-5"><div><p className="text-[10px] uppercase tracking-[0.28em] text-mocha">Gloria Hub</p><h1 className="mt-2 font-serif text-[46px] md:text-[58px] leading-none">Citas</h1><p className="mt-3 text-[13px] text-taupe">Administra cada reserva, su estado y el contacto con la clienta, todo en un solo lugar.</p></div><div className="flex gap-2"><Link href="/hub/calendar" className="inline-flex items-center gap-2 rounded-full border border-mocha/30 px-5 py-3 text-[10px] uppercase tracking-[0.14em] text-mocha"><CalendarDays size={16}/> Calendario</Link><Link href="/reservar" className="rounded-full bg-espresso px-5 py-3 text-[10px] uppercase tracking-[0.14em] text-ivory">+ Nueva Cita</Link></div></div><section className="mt-8 rounded-[24px] border border-champagne/30 bg-white/40 p-4 md:p-5"><div className="flex items-center gap-2 text-mocha"><SlidersHorizontal size={16}/><p className="text-[9px] uppercase tracking-[0.2em]">Filtros</p></div><div className="mt-4 grid gap-3 md:grid-cols-2 xl:grid-cols-6"><label className="relative xl:col-span-2"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-taupe" size={15}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar clienta, teléfono o email" className="w-full rounded-xl border border-taupe/25 bg-ivory/70 pl-9 pr-3 py-3 text-[12px] outline-none"/></label><Select value={status} onChange={setStatus} options={statuses.map(x=>[x,statusLabels[x]])}/><Select value={staffId} onChange={setStaffId} options={[["all","Todas las profesionales"],...staff.map(s=>[s.id,s.name])]}/><Select value={serviceId} onChange={setServiceId} options={[["all","Todos los servicios"],...services.map(s=>[s.id,s.name])]}/><Select value={source} onChange={setSource} options={[["all","Todos los orígenes"],["public_booking","Reserva pública"],["gloria_access","Gloria Access"],["gloria_hub","Gloria Hub"],["staff_created","Creada por staff"],["manual_admin","Manual (admin)"]]}/></div></section>{actionMessage&&<p className="mt-4 text-[12px] text-red-700">{actionMessage}</p>}<section className="mt-6 overflow-hidden rounded-[24px] border border-champagne/30 bg-white/35"><div className="hidden lg:grid grid-cols-[1.2fr_1fr_1fr_1fr_.8fr_.7fr_.8fr] gap-3 border-b border-champagne/25 px-5 py-3 text-[9px] uppercase tracking-[0.16em] text-taupe"><span>Clienta</span><span>Servicio</span><span>Profesional</span><span>Fecha y hora</span><span>Duración</span><span>Estado</span><span>Origen</span></div>{loading?<p className="p-8 text-[13px] text-taupe">Cargando citas...</p>:filtered.length===0?<p className="p-8 text-[13px] text-taupe">Ninguna cita coincide con estos filtros.</p>:filtered.map(a=><button key={a.id} onClick={()=>setSelected(a)} className="w-full text-left border-b last:border-0 border-champagne/20 px-5 py-4 hover:bg-blush/20"><div className="lg:grid grid-cols-[1.2fr_1fr_1fr_1fr_.8fr_.7fr_.8fr] gap-3 items-center"><div><p className="text-[13px] font-medium">{a.client_name}</p><p className="mt-1 text-[10px] text-taupe">{a.client_phone}</p></div><p className="mt-3 lg:mt-0 text-[12px]">{a.service?.name||"—"}</p><p className="text-[12px] text-mocha">{a.staff?.name||"—"}</p><div className="text-[11px] text-taupe"><p>{formatDate(a.start_at)}</p><p>{formatTime(a.start_at)}</p></div><p className="text-[11px] text-taupe">{minutesBetween(a.start_at,a.end_at)} min</p><Status value={a.status}/><p className="text-[10px] text-taupe">{sourceLabels[a.source]||a.source}</p></div></button>)}</section>{selected&&<div className="fixed inset-0 z-[80] bg-espresso/35 flex justify-end" onClick={()=>setSelected(null)}><aside className="h-full w-full max-w-[520px] bg-ivory p-6 md:p-8 overflow-y-auto" onClick={e=>e.stopPropagation()}><div className="flex items-start justify-between gap-4"><div><p className="text-[9px] uppercase tracking-[0.2em] text-mocha">Detalle de la cita</p><h2 className="mt-2 font-serif text-[38px] leading-none">{selected.client_name}</h2></div><button onClick={()=>setSelected(null)} className="text-[12px] text-taupe">Cerrar</button></div><div className="mt-7 space-y-6"><Detail title="Clienta"><Line label="Teléfono" value={selected.client_phone}/><Line label="Email" value={selected.client_email||"—"}/></Detail><Detail title="Cita"><Line label="Servicio" value={selected.service?.name||"—"}/><Line label="Profesional" value={selected.staff?.name||"—"}/><Line label="Fecha" value={formatDate(selected.start_at)}/><Line label="Hora" value={`${formatTime(selected.start_at)} – ${formatTime(selected.end_at)}`}/><Line label="Duración" value={`${minutesBetween(selected.start_at,selected.end_at)} min`}/><Line label="Estado" value={statusLabels[selected.status]||selected.status}/><Line label="Origen" value={sourceLabels[selected.source]||selected.source}/></Detail><Detail title="Acciones"><div className="flex flex-wrap gap-2">{selected.status==="pending"&&<Action onClick={()=>changeStatus(selected.id,"confirmed")}>Confirmar</Action>}{selected.status==="confirmed"&&<Action onClick={()=>changeStatus(selected.id,"in_progress")}>Iniciar</Action>}{["confirmed","in_progress"].includes(selected.status)&&<Link href={`/hub/appointments/${selected.id}/complete`} className="rounded-full bg-espresso px-4 py-2.5 text-[9px] uppercase tracking-[0.13em] text-ivory">Completar + Memoria</Link>}{["pending","confirmed"].includes(selected.status)&&<Action onClick={()=>changeStatus(selected.id,"cancelled")}>Cancelar</Action>}{["pending","confirmed"].includes(selected.status)&&<Action onClick={()=>changeStatus(selected.id,"no_show")}>No se presentó</Action>}<Link href={`/hub/calendar?appointment=${selected.id}`} className="rounded-full border border-mocha/30 px-4 py-2.5 text-[9px] uppercase tracking-[0.13em] text-mocha">Reprogramar</Link></div></Detail></div></aside></div>}</div>
}
function Select({value,onChange,options}:{value:string;onChange:(v:string)=>void;options:string[][]}){return <select value={value} onChange={e=>onChange(e.target.value)} className="rounded-xl border border-taupe/25 bg-ivory/70 px-3 py-3 text-[12px] outline-none">{options.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select>}
function Status({value}:{value:string}){return <span className="inline-flex w-fit rounded-full border border-champagne/40 bg-ivory px-2.5 py-1 text-[9px] uppercase tracking-[0.1em] text-mocha">{statusLabels[value]||value}</span>}
function Detail({title,children}:{title:string;children:React.ReactNode}){return <section className="border-t border-champagne/30 pt-4"><p className="text-[9px] uppercase tracking-[0.2em] text-mocha">{title}</p><div className="mt-3 space-y-2">{children}</div></section>}
function Line({label,value}:{label:string;value:string}){return <div className="flex justify-between gap-4 text-[12px]"><span className="text-taupe">{label}</span><span className="text-right">{value}</span></div>}
function Action({children,onClick}:{children:React.ReactNode;onClick:()=>void}){return <button onClick={onClick} className="rounded-full bg-espresso px-4 py-2.5 text-[9px] uppercase tracking-[0.13em] text-ivory">{children}</button>}
function formatDate(v:string){return new Date(v).toLocaleDateString("es-US",{month:"short",day:"numeric",year:"numeric",timeZone:"America/New_York"})}
function formatTime(v:string){return new Date(v).toLocaleTimeString("es-US",{hour:"numeric",minute:"2-digit",timeZone:"America/New_York"})}
function minutesBetween(a:string,b:string){return Math.round((+new Date(b)-+new Date(a))/60000)}
